# 🎯 ФИНАЛЬНОЕ ТЕХНИЧЕСКОЕ ЗАДАНИЕ (38 часов)
## "ZenBalance" — Рациональный Ассистент Импульс-Стоппер (Т-Банк)

---

## 1. ОБЗОР ПРОЕКТА

**Цель:** Создать цифрового ассистента, который **анализирует импульсивные желания** и помогает пользователю остыть перед покупкой через систему "охлаждения" (cooling-off), блокировки запрещенных категорий и ИИ-оценку целесообразности.

**Ключевое отличие:** ИС не просто считает дни, но **анализирует желание через ИИ**, сравнивает категории покупок с черным списком и дает **персональный совет** на основе финансового профиля.

**Платформы:** Web-приложение (адаптивный дизайн для мобильных + ПК).

**NO-AUTH:** Вход по никнейму без пароля.

---

## 2. ФУНДАМЕНТАЛЬНАЯ АРХИТЕКТУРА (НЕ 3-х слойная, а 4-х слойная из-за ИИ)

```
┌─────────────────────────────────────────────────────┐
│           FRONTEND (React + Vite)                   │
│  (Онбординг → Ввод товара → Результат → Личный кабинет)
└────────────────┬────────────────────────────────────┘
                 │
┌────────────────▼────────────────────────────────────┐
│      BACKEND (Node.js + Express)                    │
│  (REST API, CRUD, Бизнес-логика, Роутинг в ИИ)    │
└────────────────┬────────────────────────────────────┘
                 │
┌────────────────▼────────────────────────────────────┐
│      AI-LAYER (Python + FastAPI)                    │
│  (LLM для синонимизации, Анализ категорий,         │
│   Генерация советов, Обоснование блокировок)       │
└────────────────┬────────────────────────────────────┘
                 │
┌────────────────▼────────────────────────────────────┐
│    DATABASE (SQLite или PostgreSQL)                 │
│  (Профили, Черные списки, Wishlist, История)      │
└─────────────────────────────────────────────────────┘
```

---

## 3. MUST-HAVE ФИЧИ (Критично для 38 часов)

### 3.1 Экран Онбординга (Анкета + Настройка ИС)

**Шаг 1: Личный профиль (Форма)**
1.  **Никнейм** (Идентификация без авторизации).
2.  **Месячный доход** (в рублях) → для вычисления % бюджета.
3.  **Сумма откладываемых сбережений** (в месяц) → для расчета "комфортной" покупки.
4.  **Текущие накопления** (опционально, если включена галка "Учитывать накопления").

**Шаг 2: Настройка ИС**
1.  **Запрещенные категории** (Чекбоксы):
    *   Техника
    *   Видеоигры / Азартные игры
    *   Косметика / Шопинг одежда
    *   Еда (доставка, фастфуд)
    *   Развлечения (кино, концерты)
    *   Собственный ввод (пользователь пишет свою)
2.  **Диапазоны охлаждения** (Таблица с редактированием):
    ```
    до 15 000 ₽       → 1 день
    15 000 - 50 000   → 7 дней
    50 000 - 100 000  → 30 дней
    100 000 - 200 000 → 60 дней
    200 000+          → 90 дней
    ```
    *Пользователь может менять эти значения.*

3.  **Настройка нотификаций:**
    *   Как часто проверять wishlist (Раз в день / Раз в неделю / Раз в месяц).
    *   Какой канал (Браузерные уведомления / Email (SMTP) / Telegram).
    *   Какие товары исключить из нотификации (опционально).

**Хранилище:** LocalStorage (браузер) или простая таблица в БД.

---

### 3.2 Главный Экран (Ввод Желаемой Покупки + ИИ-Анализ)

**UI компоненты:**
1.  **Название товара** (Text Input).
2.  **Цена** (Number Input, Валюта = РУБ).
3.  **Категория** (Выпадающий список или автозаполнение).

**После нажатия "Проверить":**
1.  Фронтенд отправляет POST запрос на `/api/products/check`.
2.  Бэк отправляет данные в Python ИИ-сервис.
3.  ИИ возвращает результат с рекомендациями.

---

### 3.3 ИИ-СЛОЙ (Ключевой компонент)

**Функция 1: Синонимизация категорий (ML/AI)**

```python
# Endpoint: POST /ai/category-similarity
# Input: 
{
  "user_category": "Игровая консоль",
  "blacklist_categories": ["Техника", "Видеоигры", "Гаджеты"]
}

# Logic (LLM + Пороговое значение):
# 1. Отправить в GPT-4o-mini промпт:
#    "Сравни категорию 'Игровая консоль' с запрещенными: 'Техника', 'Видеоигры'.
#     Вернись с оценкой схожести от 0 до 1."
# 
# 2. LLM ответит: {"similarity": 0.95, "related_to": "Видеоигры, Техника"}
# 3. Если similarity > 0.7 → BLOCK (Категория близко к запрещенным)

# Output:
{
  "is_blocked": true,
  "confidence": 0.95,
  "reason": "Игровая консоль относится к категории 'Видеоигры' (запрещено)"
}
```

**Функция 2: Анализ целесообразности покупки**

```python
# Endpoint: POST /ai/purchase-advice
# Input:
{
  "product_name": "MacBook Pro",
  "price": 150000,
  "category": "Техника",
  "user_income": 100000,
  "user_savings": 80000,
  "monthly_savings": 5000,
  "cooling_period": 30
}

# Logic (LLM):
# Промпт: "Пользователь хочет купить MacBook Pro за 150k.
#          Его доход 100k, текущие накопления 80k, откладывает 5k/месяц.
#          Период охлаждения 30 дней.
#          Дай развернутый совет: стоит ли ждать, какие риски, альтернативы?"

# Output:
{
  "status": "COOLING",
  "days_to_wait": 30,
  "advice": "Смартфон стоит 15% твоего месячного дохода. Это критично. Подожди 30 дней...",
  "confidence": 0.85
}
```

**Функция 3: Генерация мотивирующего сообщения**

```python
# Endpoint: POST /ai/motivation
# Input:
{
  "action": "product_removed", // "product_removed" | "product_ready" | "impulse_detected"
  "product_name": "MacBook Pro",
  "user_nickname": "Ivan"
}

# Logic (LLM):
# Промпт: "Пользователь Ivan удалил MacBook Pro из wishlist'а.
#          Напиши короткий (20 слов) мотивирующий текст в стиле друга,
#          который понимает его финансовые цели."

# Output:
{
  "message": "Выиграл! Пропустил 15k импульса. Это +1% ближе к целям. 💪"
}
```

---

### 3.4 Бизнес-Логика (Core Algorithm на Бэке)

**Шаг 1: Проверка Черного Списка**
```javascript
async function checkProduct(productData, userProfile) {
  // Передать в ИИ для синонимизации
  const { is_blocked, confidence } = await aiService.checkCategorySimilarity(
    productData.category,
    userProfile.blacklist_categories
  );
  
  if (is_blocked && confidence > 0.7) {
    return {
      status: "BLOCKED",
      reason: "Категория в черном списке или очень похожа",
      ai_reason: is_blocked // Детальная причина от ИИ
    };
  }
  
  // Шаг 2: Определить период охлаждения
  const cooling_days = getCoolingPeriodByPrice(
    productData.price,
    userProfile.cooling_ranges
  );
  
  // Шаг 3: Если учитываются накопления
  let final_cooling_days = cooling_days;
  if (userProfile.use_savings) {
    const days_to_afford = calculateDaysToAfford(
      productData.price,
      userProfile.current_savings,
      userProfile.monthly_savings
    );
    final_cooling_days = Math.max(cooling_days, days_to_afford);
  }
  
  // Шаг 4: Получить ИИ-совет
  const advice = await aiService.getPurchaseAdvice({
    product_name: productData.name,
    price: productData.price,
    user_income: userProfile.income,
    cooling_days: final_cooling_days
  });
  
  return {
    status: "COOLING",
    cooling_days: final_cooling_days,
    unlock_date: addDays(today, final_cooling_days),
    ai_advice: advice,
    can_afford_now: userProfile.current_savings >= productData.price
  };
}
```

---

### 3.5 Экран Результата (После Анализа)

**Если статус = BLOCKED:**
```
┌────────────────────────────────────────┐
│  🚫 ПОКУПКА ЗАБЛОКИРОВАНА              │
├────────────────────────────────────────┤
│  Категория "Видеоигры" в черном списке │
│                                        │
│  💡 Совет от ИИ:                      │
│  "Игры — твоя главная слабость.       │
│   Давай сосредоточимся на важном."    │
│                                        │
│  [Вернуться] [Изменить профиль]       │
└────────────────────────────────────────┘
```

**Если статус = COOLING:**
```
┌────────────────────────────────────────┐
│  ⏱️ ПЕРИОД ОХЛАЖДЕНИЯ: 30 ДНЕЙ        │
├────────────────────────────────────────┤
│  MacBook Pro — 150 000 ₽               │
│  Разблокировка: 04.01.2026             │
│                                        │
│  📊 Рекомендация ИИ:                  │
│  "Это 15% твоего дохода. Подожди      │
│   месяц. За это время накопишь еще     │
│   5k, и покупка будет комфортнее."    │
│                                        │
│  Статус: Накоплено 80k (需要 70k ещё) │
│                                        │
│  [Добавить в Wishlist] [Забыть]       │
└────────────────────────────────────────┘
```

---

### 3.6 Личный Кабинет (Wishlist + История + Профиль)

**Вкладка 1: Wishlist (Активные товары)**
```
Товар                 Цена      Статус         Разблокировка
─────────────────────────────────────────────────────────────
MacBook Pro          150 000   ЖДЁТ (20 дн)   04.01.2026
Кроссовки            8 000     ГОТОВО!         ✅
Наушники             12 000    ЖДЁТ (7 дн)    28.12.2025
```

Кнопки: Купить / Удалить / Отложить ещё на неделю.

**Вкладка 2: История**
```
Действие                    Товар              Дата
──────────────────────────────────────────────────────
✅ Купил                    Наушники JBL        20.12.2025
❌ Отменил                  Кино билеты         15.12.2025
⏸️ Отложил на неделю        Косметика MAC       10.12.2025
```

**Вкладка 3: Профиль**
```
Никнейм: Ivan
Месячный доход: 100 000 ₽
Откладываю в месяц: 5 000 ₽
Текущие накопления: 80 000 ₽

Запрещенные категории: Видеоигры, Косметика, Техника
Период охлаждения по цене: [Редактировать]
Нотификации: Каждый день (Email)

[Сохранить изменения]
```

---

### 3.7 Система Нотификаций (ИИ-Powered)

**Что происходит:**
1.  Крон-задача каждый день в 09:00 проверяет wishlist.
2.  Для каждого товара ИИ генерирует персональное напоминание.
3.  Отправляет уведомление (браузер / Email / Telegram).

**Пример напоминания (сгенерировано ИИ):**
```
Привет, Ivan! 👋

Помнишь про MacBook Pro (150k)?
Осталось ждать 20 дней (разблокировка 04.01).

Пока ты ждал, накопил еще 3k. Молодец! 💪
Осталось 67k (нужно 70k для комфорта).

А может, посмотреть что-то дешевле?
MacBook Air стоит 90k — хватит уже сейчас!

👉 [Проверить wishlist]
```

**Каналы нотификации (для SHOULD):**
- Браузер (Notification API) — MUST
- Email (SMTP) — SHOULD
- Telegram бот — COULD

---

## 4. АРХИТЕКТУРА ИИ-СЕРВИСА (Python)

**Технологии:**
- FastAPI (для REST API)
- OpenAI API (GPT-4o-mini для LLM)
- LangChain или raw `openai` lib

**Структура:**
```
backend-ai/
├── main.py                # FastAPI app
├── routes/
│   ├── category.py       # POST /category-similarity
│   ├── purchase.py       # POST /purchase-advice
│   └── motivation.py     # POST /motivation
├── prompts/
│   ├── category_check.txt
│   ├── purchase_advice.txt
│   └── motivation_templates.txt
├── config.py             # Настройки LLM, токены
└── utils.py              # Парсинг ответов, валидация
```

**Промты (Примеры):**

```text
# category_check.txt
You are a financial category analyzer.
Compare the user's desired product category with their blacklist.

User category: {user_category}
Blacklist: {blacklist}

Return JSON:
{
  "is_blocked": true/false,
  "similarity": 0.0-1.0,
  "reason": "short explanation"
}
Only JSON, no markdown.
```

```text
# purchase_advice.txt
You are a personal finance coach.
User wants to buy: {product_name} for {price} rubles.

Context:
- Monthly income: {income}
- Current savings: {savings}
- Monthly savings: {monthly_savings}
- Cooling period: {cooling_days} days

Give SHORT (max 50 words) advice in Russian.
Be empathetic but honest about risks.
```

---

## 5. API ENDPOINTS (Backend)

### Authentication & Profile
```
POST /api/auth/login
{ "nickname": "Ivan" }
← { "user_id": 1, "profile": {...} }

GET /api/profile/:user_id
← { "income": 100000, "blacklist": [...], ... }

PUT /api/profile/:user_id
{ "income": 120000, "cooling_ranges": [...] }
← { "success": true }
```

### Product Analysis (+ ИИ)
```
POST /api/products/check
{
  "user_id": 1,
  "product_name": "MacBook Pro",
  "price": 150000,
  "category": "Техника"
}
←
{
  "status": "COOLING",
  "cooling_days": 30,
  "unlock_date": "2026-01-04",
  "ai_blocked_reason": null,
  "ai_advice": "Это 15% твоего дохода...",
  "can_afford_now": false,
  "days_to_afford": 14
}
```

### Wishlist Management
```
POST /api/wishlist/:user_id
{ "product_name": "MacBook", "price": 150000, "category": "Техника" }
← { "id": 1, "unlock_date": "2026-01-04", "status": "waiting" }

GET /api/wishlist/:user_id
← [
  { "id": 1, "product": "MacBook", "status": "waiting", ... },
  { "id": 2, "product": "Кроссовки", "status": "ready", ... }
]

DELETE /api/wishlist/:user_id/:product_id
← { "success": true }
```

### AI Endpoints (Backend ↔ Python)
```
POST /ai/category-similarity
(Внутренний, только для бэка)

POST /ai/purchase-advice
(Внутренний, только для бэка)

POST /ai/generate-notification
(Для крон-задачи отправки уведомлений)
```

---

## 6. БД SCHEMA (Минимум)

```sql
CREATE TABLE users (
  id INT PRIMARY KEY AUTO_INCREMENT,
  nickname VARCHAR(50) UNIQUE NOT NULL,
  monthly_income INT,
  monthly_savings INT,
  current_savings INT,
  use_savings_calculation BOOLEAN DEFAULT false,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE user_settings (
  user_id INT PRIMARY KEY,
  blacklist_categories JSON, -- ["Техника", "Видеоигры", ...]
  cooling_ranges JSON, -- [{"min": 0, "max": 15000, "days": 1}, ...]
  notification_frequency VARCHAR(20), -- "daily" | "weekly" | "monthly"
  notification_channel VARCHAR(20), -- "browser" | "email" | "telegram"
  notification_excluded_products JSON,
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
);

CREATE TABLE wishlist (
  id INT PRIMARY KEY AUTO_INCREMENT,
  user_id INT NOT NULL,
  product_name VARCHAR(200),
  price INT,
  category VARCHAR(50),
  cooling_period_days INT,
  unlock_date DATE,
  status ENUM('waiting', 'ready', 'bought', 'cancelled') DEFAULT 'waiting',
  ai_recommendation TEXT, -- Сохраняем совет от ИИ
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
  INDEX (user_id, status)
);

CREATE TABLE history (
  id INT PRIMARY KEY AUTO_INCREMENT,
  user_id INT NOT NULL,
  action VARCHAR(50), -- "bought" | "cancelled" | "removed"
  product_name VARCHAR(200),
  price INT,
  action_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id)
);
```

---

## 7. ПЛАН РЕАЛИЗАЦИИ (38 часов)

| Время | Задача | Кто | Статус |
|:---:|:---|:---|:---:|
| **0-4ч** | Дизайн макета + БД схема | Designer + SA | 📋 |
| **4-8ч** | Онбординг фронт + LocalStorage | React Junior | 💻 |
| **8-12ч** | Backend scaffold + Auth (nickname) | Fullstack | 💻 |
| **12-16ч** | Главный экран (фронт) + API профиля | React Middle | 💻 |
| **16-20ч** | ИИ-сервис (Python) + Промты | ML/SA | 🤖 |
| **20-24ч** | Интеграция фронта с ИИ (бэк роутер) | Fullstack | 🔗 |
| **0-4ч (ночь)** | Wishlist (фронт + бэк) | React Junior + Fullstack | 💻 |
| **4-8ч** | Нотификации (браузер API) | React Middle | 📬 |
| **8-12ч** | Личный кабинет (История + Профиль) | React Junior | 💻 |
| **12-16ч** | Полировка UI + адаптив мобильный | React Middle + Designer | 🎨 |
| **16-18ч** | Тестирование, Баг-фиксы | Все | 🐛 |
| **18-20ч** | Swagger документация | Fullstack | 📚 |
| **20-22ч** | Демо-сценарий + Скринкаст | PM | 📹 |
| **22-24ч** | Репетиция презентации | Все | 🎤 |
| **24-38ч** | **БУФЕР** | - | ✅ |

---

## 8. MUST vs SHOULD vs COULD

### MUST (Без этого = 0 баллов)
- ✅ Онбординг (Профиль + Черный список).
- ✅ Ввод желаемой покупки (Название, Цена, Категория).
- ✅ Расчет периода охлаждения (По цене).
- ✅ Расчет период охлаждения (По накоплениям, если включено).
- ✅ Проверка черного списка (Базовая, без ИИ — можно захардкодить).
- ✅ Wishlist (Сохранение товаров + Таймеры).
- ✅ Результат (Экран с рекомендацией).
- ✅ Личный кабинет (Без авторизации, по никнейму).
- ✅ Swagger/OpenAPI документация.

### SHOULD (Добавляет очки, но необязательно)
- 🤖 **ИИ синонимизация категорий** (LLM).
- 🤖 **ИИ совет по покупке** (LLM генерирует текст).
- 📬 Браузерные нотификации (Notification API).
- 📬 Email уведомления (SMTP).
- 📊 История (Купил / Отменил).

### COULD (Вишенка на торте, если время)
- 📱 Telegram бот (отдельная реализация, требует времени).
- 🔗 Парсинг ссылок на товары.
- 💬 Чат-бот для онбординга (вместо анкеты).
- 📧 Telegram нотификации.

---

## 9. КАК ИСПОЛЬЗОВАТЬ ИИ (GPT-4o-mini) ДЛЯ ПОБЕДЫ

**Сценарий 1: Юзер хочет купить "Игровую консоль", а "Видеоигры" в черном списке**
```
LLM Промпт: "User blacklist: ['Видеоигры', 'Азартные игры'].
             Product category: 'Игровая консоль'.
             Are they related? Return JSON: {is_related: bool, confidence: 0-1}"

LLM Response: {
  "is_related": true,
  "confidence": 0.95,
  "reason": "Game console is a hardware specifically designed for video games"
}

Action: BLOCK + Message: "Игровые консоли относятся к видеоиграм (запрещено)"
```

**Сценарий 2: Юзер добавил товар в Wishlist, и каждый день приходит ИИ-напоминание**
```
Крон-задача (каждый день 09:00):
1. Проверить wishlist пользователя
2. Для каждого товара, который ждет разблокировки:
   - Дни осталось до разблокировки?
   - Накопил ли юзер деньги?
   - Отправить ИИ-напоминание

LLM Промпт: "User Ivan has saved 85k out of 150k needed for MacBook Pro.
             15 days left until cooling-off ends.
             Monthly savings: 5000.
             Generate a SHORT motivational message (max 40 words) in Russian.
             Be supportive but honest."

LLM Response: "Привет, Ivan! 💪 Осталось 15 дней. Если отложишь еще 2 недели,
              хватит на MacBook. Или посмотреть Mac Mini (дешевле)?"
```

---

## 10. КРИТЕРИИ ОЦЕНКИ ЖЮРИ

### ✅ За что дают 100 баллов:
1.  **Рабочий MVP:** Все MUST-фичи работают без ошибок.
2.  **ИИ-компонент:** LLM реально используется (синонимизация, советы).
3.  **Логика "охлаждения":** Правильный расчет дней (по цене И накоплениям).
4.  **Черный список:** Категория в черном списке → автоматическая блокировка.
5.  **Wishlist:** Сохранение, таймеры, история действий.
6.  **Без авторизации:** Просто ник, никаких паролей.
7.  **Мобильный адаптив:** Работает на телефоне (сетка, кнопки, читаемо).
8.  **Swagger:** Полная документация API.
9.  **UI/UX:** Понятный интерфейс, логичный флоу.

### ❌ За что дают 0 баллов:
- Только макет в Figma (без кода).
- Telegram-бот вместо веб-приложения.
- Не работает без интернета (нет локального хранилища).
- Требует регистрацию (авторизация через email/SMS).
- ИИ не интегрирован (обещали, но не сделали).

---

## 11. СТЕК ТЕХНОЛОГИЙ (Оптимальный)

| Компонент | Выбор | Почему |
|:---|:---|:---|
| **Frontend Framework** | React 18 + Vite | Быстро, современно, простая конфигурация |
| **State Management** | Zustand или Jotai | Лёгкие, без Redux бойлерплейта |
| **UI Components** | shadcn/ui или MUI | Готовые компоненты, адаптивность |
| **HTTP Client** | axios | Простой, надежный |
| **Backend** | Node.js + Express | Быстро, JavaScript, много библиотек |
| **Database** | SQLite (dev) / PostgreSQL (prod) | Для хакатона SQLite хватает |
| **AI Service** | Python + FastAPI | Стандарт для LLM интеграций |
| **LLM Provider** | OpenAI (GPT-4o-mini) | Дешево ($5 за весь хакатон), быстро |
| **Notifications** | Notification API (браузер) | Встроено в браузер, бесплатно |
| **Deployment** | Vercel (frontend) + Railway/Render (backend) | Бесплатно для хакатонов |

---

## 12. QUICK-START (Команда Должна Начать СЕЙЧАС)

### Frontend (React)
```bash
npm create vite@latest frontend -- --template react
cd frontend
npm install
npm install zustand axios @mui/material @emotion/react @emotion/styled
npm install -D tailwindcss postcss autoprefixer

# src/main.jsx
# src/App.jsx (Онбординг → Главный → Результат → ЛК)
```

### Backend (Node.js)
```bash
mkdir backend && cd backend
npm init -y
npm install express cors sqlite3 dotenv axios

# server.js
# routes/auth.js (nickname login)
# routes/products.js (POST /check)
# routes/wishlist.js (GET, POST, DELETE)
```

### AI Service (Python)
```bash
mkdir backend-ai && cd backend-ai
python -m venv venv && source venv/bin/activate
pip install fastapi uvicorn openai pydantic python-dotenv

# main.py
# routes/category.py (LLM для синонимизации)
# routes/purchase.py (LLM для совета)
# prompts/
```

---

## 13. КРАСНЫЕ ФЛАГИ (Что НЕ делать)

🚫 **Не делайте:**
- Авторизацию (OAuth, email, SMS) — просто ник.
- Telegram-бот как основное решение.
- Парсинг ссылок на товары (скоп слишком большой).
- Интеграцию с реальными банками (API сложные).
- Фейковые данные на фронте (работайте с реальным бэком с часа 4).

✅ **Делайте:**
- Mock данные только для быстрого тестирования.
- Реальный REST API с первого дня.
- Простой, понятный UI (не переусложняйте дизайн).
- Тестируйте ИИ с реальными промтами (не забывайте про `response_format: json`).
- Коммитьте в гит каждый час (на случай сбоев).

---

## 14. ПОБЕДНЫЙ СЦЕНАРИЙ (Финал презентации)

**Демонстрация (5 минут):**
1.  Открываем приложение, вводим никнейм "Judge".
2.  Заполняем профиль: доход 100k, откладываю 5k/месяц, черный список = [Игры, Техника].
3.  Вводим "Игровая консоль за 50k".
4.  **ИИ говорит:** "Игровая консоль = Видеоигры (запрещено). + Это 50% твоего месячного дохода."
5.  Приложение блокирует покупку.
6.  Открываем Wishlist, там история отмен и советы.
7.  Показываем Swagger с полной документацией API.

**Слова на презентации:**
> "Мы создали Рациональный Ассистент, который анализирует твои желания через ИИ и не дает совершить ошибку. Это не просто таймер, это психолог твоего кошелька."

---

**Удачи! Вы готовы к хакатону! 🚀**

